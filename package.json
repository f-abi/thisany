{
  "name": "thisany",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:media": "turbo run dev --filter=media",
    "dev:m": "turbo run dev --filter=media",
    "dev:website": "turbo run dev --filter=website",
    "dev:w": "turbo run dev --filter=website",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "fmt": "prettier --write .",
    "clean": "rm -rf node_modules pnpm-lock.yaml",
    "cz": "czg",
    "prepare": "husky"
  },
  "packageManager": "pnpm@10.25.0",
  "devDependencies": {
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:",
    "czg": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "prettier": "catalog:",
    "prettier-plugin-tailwindcss": "catalog:",
    "turbo": "catalog:"
  }
}
